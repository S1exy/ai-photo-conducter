services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ai_image_template
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ai-image-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ai_image_template"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  ai-image-postgres:
